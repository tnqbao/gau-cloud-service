apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gau-cloud-ingress
  namespace: bao-${DEPLOY_ENV}-env
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: ${DOMAIN_NAME}
      http:
        paths:
          - path: /api/v1/cloud/
            pathType: Prefix
            backend:
              service:
                name: gau-cloud-http-service
                port:
                  number: 8080