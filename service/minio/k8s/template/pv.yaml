apiVersion: v1
kind: PersistentVolume
metadata:
  name: gau-temp-minio-pv
  labels:
    app: bao-platform
    env: ${DEPLOY_ENV}
spec:
  capacity:
    storage: 50Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: temp-minio-local
  local:
    path: /mnt/temp-object-storage
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - agent-8