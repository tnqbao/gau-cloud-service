apiVersion: apps/v1
kind: Deployment
metadata:
  name: gau-garage-deployment
  namespace: bao-${DEPLOY_ENV}-env
  labels:
    app: bao-platform
    env: ${DEPLOY_ENV}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gau-garage-service
  template:
    metadata:
      labels:
        app: gau-garage-service
        env: ${DEPLOY_ENV}
    spec:
      containers:
        - name: garage
          image: dxflrs/garage:v1.0.0
          imagePullPolicy: IfNotPresent
          command: ["/garage"]
          args: ["-c", "/etc/garage.toml", "server"]

          ports:
            - name: s3-api
              containerPort: 3900
            - name: rpc
              containerPort: 3901
            - name: web
              containerPort: 3902

          env:
            - name: GARAGE_RPC_SECRET
              valueFrom:
                secretKeyRef:
                  name: gau-garage-${DEPLOY_ENV}-secret
                  key: GARAGE_RPC_SECRET

          volumeMounts:
            - name: garage-data
              mountPath: /data
            - name: garage-meta
              mountPath: /meta
            - name: garage-config
              mountPath: /etc/garage.toml
              subPath: garage.toml

          resources:
            requests:
              cpu: "100m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"

      volumes:
        - name: garage-data
          persistentVolumeClaim:
            claimName: gau-garage-pvc
        - name: garage-meta
          emptyDir: {}
        - name: garage-config
          configMap:
            name: gau-garage-${DEPLOY_ENV}-config
            items:
              - key: garage.toml
                path: garage.toml
